apiVersion: v1
kind: Service
metadata:
  name: votingappresult-service
  labels:
    name: votingappresult-service
    type: frontend
spec:
  type: NodePort
  selector:
    name: voting-app-pod
    type: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008